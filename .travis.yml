dist: trusty
language: cpp
install: make
addons:
    apt:
        packages:
            - cmake-data
            - cmake
compiler: 
    - clang
    - gcc
script:
    - cd build
    - cmake -DBUILD_BENCHMARKS=OFF ..
    - make
    - ./tests/vector_tests
